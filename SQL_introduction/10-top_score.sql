-- A script that lists all records of the table second_table and orders them in descedning order
-- Query to list all records of the second_table
SELECT score, name FROM second_table
ORDER BY score DESC
